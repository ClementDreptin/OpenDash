# Images

The PNG images from this directory were created from the SVG files with Inkscape. To changes the images, modify the SVG files and run this command to generate the corresponding PNG file.

```
inkscape /path/to/svg -o /path/to/png
```
